syntax = "proto3";

// package proto;
option go_package=".;driver";

service Greeter {
    rpc HelloTest (SayRequest) returns (SayResponse);
}

message  SayRequest{
    string name = 1;
    bytes data = 2;
}

message SayResponse{
    string greeting = 2;
}