syntax="proto3";

// package lbs;
option go_package = "./proto;proto";

service LBSSrv {
    rpc DriverLocations(Request) returns (Response) {}
}

message Request {
    string name = 1;
}

message Response {
    string msg = 1;
}

// protoc --proto_path=$GOPATH/src:. --micro_out=. --go_out=. proto/lbs.proto